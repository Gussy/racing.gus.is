[defaults]
inventory = inventory/
roles_path = roles
vault_password_file = ../scripts/vault-pass.sh
pipelining = True
retry_files_enabled = False

[privilege_escalation]
become = True
become_method = sudo
