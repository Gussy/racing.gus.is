{{ domain }} {
    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains"
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
    }

    handle /grafana/* {
        reverse_proxy localhost:{{ grafana_port }}
    }

    handle {
        reverse_proxy localhost:{{ racetelem_port }}
    }
}
